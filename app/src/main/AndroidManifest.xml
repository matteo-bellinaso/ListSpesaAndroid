<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.matteobellinaso.listspesaandroid">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SpesApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity android:name=".ui.activity.MainActivity"></activity>
        <activity android:name=".ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.RegistrazioneActivity"></activity>
        <activity android:name=".ui.activity.TutorialActivity"></activity>
        <activity android:name=".ui.activity.ListActivity"></activity>
        <activity android:name=".ui.activity.ProfileActivity"></activity>
        <activity android:name=".ui.activity.DetailActivity"></activity>
    </application>

</manifest>